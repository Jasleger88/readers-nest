<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Readers Data Sheet</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
  <h1>Reader Details</h1>
    <%- include('./partials/nav') %>
    <h2><%= reader.name %></h2>
    <p><%= reader.title %></p>
    <p><%= reader.level %></p>
    <form action="/reader/<%=reader._id %>?_method=DELETE" method="POST">
      <button type="submit">Delete <%= reader.name %></button>
    </form>
    <a href="/reader/<%= reader._id %>/edit">Edit <%= reader.name %></a>
    <a href="/reader">Back to Workout Needs</a>
</body>

</html>